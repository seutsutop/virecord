<Window x:Class="VIrecord.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VIrecord" Height="200" Width="350"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        Background="#2D3436" Topmost="True">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="VIrecord" FontSize="24" FontWeight="Bold"
                   Foreground="#A29BFE" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- Status -->
        <TextBlock x:Name="TxtStatus" Grid.Row="1" Text="⏹ STOPPED"
                   FontSize="18" Foreground="#DFE6E9" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- Record Button -->
        <Button x:Name="BtnRecord" Grid.Row="2" Content="● REC" FontSize="16"
                Background="#6C5CE7" Foreground="White" Padding="30,10"
                BorderThickness="0" Cursor="Hand" Click="BtnRecord_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="5" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

        <!-- Settings hint -->
        <TextBlock Grid.Row="3" Text="FFmpeg required in PATH"
                   FontSize="10" Foreground="#636E72" VerticalAlignment="Bottom"/>
    </Grid>
</Window>